/* Compilação simples */
const nome = "Edio";
const sobrenome = "Junior";
const curso = "typescript";

console.log(nome);
console.log(sobrenome);
console.log(curso);

// - Para rodar o arquivo vamos primeiro compilar o .ts para .js 'tsc aula02.ts'. Desta forma será criado um arquivo .js;
// - Vamos rodar o arquivo .js 'node aula02.js';
// - Para visualizar as alterações precisamos rodar novamente os 2 arquivos.
